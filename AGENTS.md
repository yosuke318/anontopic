# AGENTS.md

このリポジトリでコードを書く AI エージェント向けの指示。人間の開発者向けの内容も含め、
詳細は [CONTRIBUTING.md](./CONTRIBUTING.md) を参照すること。

## 必ず守ること

1. **モジュール間の依存ルール**（CONTRIBUTING.md「モジュール間の依存ルール」）
   `internal/` 配下のモジュールは、他モジュールの DB モデル / リポジトリを直接 import しない。
2. **コメントの書き方**（CONTRIBUTING.md「コメントの書き方」）
   - 過去の実装と比較しない（従来 / 以前 / 旧〜 / 置換 / 〜と同じ / previously / instead of など）。
   - 造語を作らない。コメントで使う用語は、コード上の識別子・設計書で定義済みの用語・
     一般的な技術用語のいずれかに限る。
3. **設計判断は ADR に残す**（`.claude/skills/adr`）
   技術選定・データモデル・外部インターフェースの決定など、後から「なぜこうしたか」が
   必要になる判断は `docs/adr/` に記録し、その決定を実装する PR に含める。
4. **push 前に `make check` が通ること。**
